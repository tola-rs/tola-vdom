[package]
name = "tola-vdom-macros"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Proc macros for tola-vdom custom families"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing", "extra-traits"] }
